import 'package:flutter/material.dart';

class PaddingSection extends StatelessWidget {
  const PaddingSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
